{
  "properties": {
    "demonstration": {
      "description": "Product-level proof plan. Every product acceptance outcome must have exactly one product_evidence binding.",
      "properties": {
        "gate_ids": {
          "description": "Gate IDs that run in the product demonstration lane.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "product_evidence": {
          "description": "One exact proof binding for every product acceptance outcome. Copy each outcome text exactly and bind only gate IDs that genuinely prove that outcome.",
          "items": {
            "properties": {
              "gate_ids": {
                "description": "Specific configured gate IDs whose passing results are evidence for this exact outcome. Observable user-journey, browser, auth, security, deploy, or visual outcomes must include at least one gate also listed in demonstration.gate_ids.",
                "items": {
                  "type": "string"
                },
                "minItems": 1,
                "type": "array"
              },
              "outcome": {
                "description": "Copy one product acceptance_outcomes string exactly, without paraphrasing.",
                "minLength": 1,
                "type": "string"
              }
            },
            "required": [
              "outcome",
              "gate_ids"
            ],
            "type": "object"
          },
          "minItems": 1,
          "type": "array"
        },
        "required": {
          "type": "boolean"
        }
      },
      "required": [
        "required",
        "gate_ids",
        "product_evidence"
      ],
      "type": "object"
    },
    "global_invariants": {
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "type": "array"
    },
    "summary": {
      "type": "string"
    },
    "tasks": {
      "items": {
        "properties": {
          "acceptance": {
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "type": "array"
          },
          "allowed_paths": {
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "type": "array"
          },
          "context_paths": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "dependencies": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "gate_ids": {
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "type": "array"
          },
          "goal": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "risk": {
            "enum": [
              "low",
              "medium",
              "high"
            ],
            "type": "string"
          },
          "title": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "goal",
          "dependencies",
          "allowed_paths",
          "context_paths",
          "acceptance",
          "gate_ids",
          "risk"
        ],
        "type": "object"
      },
      "minItems": 1,
      "type": "array"
    }
  },
  "required": [
    "summary",
    "tasks",
    "demonstration",
    "global_invariants"
  ],
  "type": "object"
}
